# NetworkManager: Hotspot-Interface nicht verwalten
# Installiert nach: /etc/NetworkManager/conf.d/99-fotoserver.conf
# Ohne diese Konfiguration würde NetworkManager das Interface zurückfordern
# und hostapd beim Start scheitern lassen.

[keyfile]
unmanaged-devices=interface-name:__INTERFACE__
